{
  "name": "deepseek-web-bridge-sidecar",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright sidecar for deepseek-web-bridge — browser automation for DeepSeek web chat",
  "license": "Apache-2.0",
  "main": "sidecar.js",
  "scripts": {
    "start": "node sidecar.js",
    "check": "node --check sidecar.js && node --check providers.js && node --check bridge.js && node --check webdriver.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
